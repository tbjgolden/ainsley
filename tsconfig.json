// This is used by projects that find and use the "tsconfig.json" file
{
  "extends": "./tsconfig.build.json",
  "exclude": ["node_modules"],
  "include": ["scripts", "src", "test"],
  "compilerOptions": {
    "allowJs": true,
    "declaration": false,
    "declarationDir": null,
    "module": "commonjs"
  }
}
